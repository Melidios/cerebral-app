# Cerebral coding challenge

### Prerequisites

 - PostgresQL


### Steps to Setup

1. Clone the repo
2. Navigate to the root directory 
3. Run `bundle install` 
4. Run `rails db:setup`
5. Run `rails s`

